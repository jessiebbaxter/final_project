<div class="wrapper">
  <div class="container">
    <h3></h3>
    <%= render 'shared/search' %>
    <div class="products-container">
      <h2><strong>Results</strong></h2>
      <%= render "shared/products_count" %>
      <div class="cards-container row">
        <% @products.each do |product| %>
          <% @price_array = [] %>
          <%= render "shared/card-stretch", product: product %>
        <% end %>
      </div>
    </div>
  </div>
</div>
